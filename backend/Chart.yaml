apiVersion: v2
name: backend
description: Helm chart for Corona Tracker Backend
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: mongodb
    version: "16.x.x"
    repository: https://charts.bitnami.com/bitnami
